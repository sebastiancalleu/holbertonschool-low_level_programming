#include "holberton.h"
/**
 * infinite_add - this function add an infinite number.
 * @n1: number to add.
 * @n2: number to add.
 * @r: result number.
 * @size_r: size of the result
 * Return: this is the sum.
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
return (r);
}
