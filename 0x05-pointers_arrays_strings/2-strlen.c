#include "holberton.h"
/**
 * _strlen - this function prints the lenght of a string
 * @s: input string.
 * Return: is the result
 */
int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; ++i)
{
}
return (i);
}
