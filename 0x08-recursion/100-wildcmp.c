#include "holberton.h"
/**
 * wildcmp - this function compares two strings.
 * @s1: string 1.
 * @s2: string 2.
 * Return: 1 if match, 0 if doesnt match.
 */

int wildcmp(char *s1, char *s2)
{
	if (s1 != s2)
		return (1);
	return (0);
}

